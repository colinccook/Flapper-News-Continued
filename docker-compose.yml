version: '3'

services:
  service:
    build: ./service
    volumes:
      - ./service:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 3000:3000
    depends_on:
      - database
  database:
    image: mongo
    ports:
      - 27017:27017
    